# 项目启动说明

## 项目结构

```
qimo2/
├── backend/    ← Python Flask 后端（REST API）
└── frontend/   ← Vue 3 前端（Vite 开发服务器）
```

> ⚠️ **常见错误**：不要在 `frontend/` 目录下执行 `python app.py`，前端是 Vue 项目，不是 Python 项目！

---

## 第一步：启动后端（必须先启动）

打开 **第一个 PowerShell 窗口**，执行：

```powershell
# 切换到后端目录
cd C:\Users\20231\Desktop\人机交互\qimo2\backend

# 用虚拟环境的 Python 启动 Flask（推荐方式）
C:\Users\20231\.workbuddy\binaries\python\envs\default\Scripts\python.exe app.py
```

**启动成功的标志**（你会看到类似输出）：

```
FTP服务器已启动，监听 0.0.0.0:2121
 * Running on http://127.0.0.1:5000
```

验证后端正常：打开浏览器访问 http://127.0.0.1:5000/api/health，返回 `{"status":"ok"}` 即为成功。

---

## 第二步：启动前端

打开 **第二个 PowerShell 窗口**，执行：

```powershell
# 切换到前端目录
cd C:\Users\20231\Desktop\人机交互\qimo2\frontend

# 启动 Vue 开发服务器
npm run dev
```

> 如果 npm 命令找不到，用完整路径：
> ```powershell
> "C:\Program Files\nodejs\npm.cmd" run dev
> ```

**启动成功的标志**：

```
  VITE v5.x.x  ready in 500ms

  ➜  Local:   http://localhost:3000/
```

---

## 第三步：打开浏览器

访问 **http://localhost:3000**

默认账号（第一次需要注册）：
- 注册页面：点击登录页的"注册"链接
- 用任意用户名和密码注册即可

---

## 注意事项

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 前端无法连接后端 | 后端没启动 | 先启动后端 |
| Token 失效需重新登录 | 已修复（密钥固定化） | 重启后端即可，不会再失效 |
| FTP 连接失败 | 内置 FTP 服务器在后端启动时自动启动 | 确保后端正常运行 |
| 端口被占用 | 5000 或 3000 被其他程序占用 | 关闭占用程序或重启电脑 |

---

## 端口说明

| 端口 | 用途 |
|------|------|
| 5000 | Flask 后端 API |
| 2121 | 内置 FTP 服务器 |
| 3000 | Vue 前端开发服务器 |

---

## 快速验证命令

```powershell
# 测试后端是否正常
curl http://127.0.0.1:5000/api/health

# 测试 FTP 是否正常
curl http://127.0.0.1:5000/api/ftp/builtin/status
```
