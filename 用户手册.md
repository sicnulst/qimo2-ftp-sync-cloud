# FTP私有同步云盘系统 - 用户手册

## 一、这个系统是干什么的？

### 简单理解

想象你有两台电脑（比如家里一台、办公室一台），你想让两台电脑上的某个文件夹保持同步——在家里改了文件，到办公室自动就有最新版本。

这个系统就是解决这个问题的：

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  电脑A      │  ──────> │  FTP服务器  │ <────── │  电脑B      │
│  (家里)     │  上传    │  (云端)     │  下载   │  (办公室)   │
└─────────────┘         └─────────────┘         └─────────────┘
```

**核心功能：**
- 你指定一个本地文件夹（比如 `D:\我的文档`）
- 系统自动监控这个文件夹的变化
- 一旦有文件新增、修改、删除，自动同步到FTP服务器
- 其他电脑连接同一个FTP服务器，也能自动同步

### 和百度网盘/坚果云的区别

| 特点 | 百度网盘 | 本系统 |
|------|----------|--------|
| 服务器 | 百度的服务器 | 你自己的电脑/服务器 |
| 隐私 | 文件上传到百度 | 文件在你自己的地盘 |
| 费用 | 会员收费 | 免费 |
| 速度 | 受限 | 局域网内超快 |
| 定制 | 固定功能 | 完全可控 |

**适用场景：**
- 公司内部文件同步
- 个人多台电脑文件同步
- 需要隐私保护的文件备份
- 开发代码同步

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      你的电脑                                │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  本地文件夹  │───>│  watchdog   │───>│  同步引擎   │     │
│  │  D:\我的文档 │    │  文件监控    │    │  比对+传输  │     │
│  └─────────────┘    └─────────────┘    └──────┬──────┘     │
│                                               │             │
│                                               ▼             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  Web界面    │<───│  Flask后端  │<───│  FTP客户端  │     │
│  │  浏览器管理  │    │  API服务    │    │  上传下载   │     │
│  └─────────────┘    └─────────────┘    └──────┬──────┘     │
│                                               │             │
└───────────────────────────────────────────────┼─────────────┘
                                                │
                                                ▼
                                       ┌─────────────┐
                                       │  FTP服务器  │
                                       │  存储文件   │
                                       └─────────────┘
```

**组成模块：**

1. **本地文件夹** - 你要同步的文件夹
2. **watchdog监控** - 实时监控文件夹变化
3. **同步引擎** - 决定怎么同步（策略）
4. **FTP客户端** - 负责上传/下载文件
5. **FTP服务器** - 存储文件的地方
6. **Flask后端** - 提供API接口
7. **Web界面** - 浏览器中管理一切

---

## 三、功能详解

### 1. 用户管理

**注册账号**
- 打开系统，点击"注册"
- 输入用户名（至少3位）和密码（至少6位）
- 注册后自动登录

**登录系统**
- 输入用户名和密码
- 登录后进入控制面板

---

### 2. FTP服务器管理

**什么是FTP服务器？**
- FTP是一种文件传输协议
- FTP服务器就是存放文件的"仓库"
- 你可以连接多个FTP服务器

**添加服务器**
1. 点击左侧菜单"FTP服务器"
2. 点击"添加服务器"
3. 填写信息：
   - **名称**：给服务器起个名字（如"公司云盘"）
   - **主机地址**：服务器IP（如127.0.0.1是本机）
   - **端口**：默认2121
   - **用户名**：FTP账号
   - **密码**：FTP密码
   - **远程路径**：服务器上的文件夹路径
4. 点击"确定"

**测试连接**
- 点击"测试连接"按钮
- 如果显示"连接成功"，说明配置正确
- 如果失败，检查地址、端口、用户名、密码

**浏览远程文件**
- 点击"浏览文件"
- 可以查看服务器上的文件和文件夹
- 点击文件夹可以进入子目录

---

### 3. 同步任务管理（核心功能）

**什么是同步任务？**
- 同步任务 = 本地文件夹 + FTP服务器 + 同步规则
- 告诉系统：监控哪个本地文件夹，同步到哪个服务器

**创建任务**
1. 点击左侧菜单"同步任务"
2. 点击"创建任务"
3. 填写信息：
   - **FTP服务器**：选择要同步到哪个服务器
   - **本地路径**：你要同步的文件夹（如 `D:\我的文档`）
   - **远程路径**：服务器上的目标文件夹（默认 `/`）
   - **同步策略**：怎么同步（见下文）
   - **扫描间隔**：多久检查一次变化（默认60秒）
   - **自动启动**：创建后是否自动开始监控
4. 点击"创建"

**同步策略（重要！）**

系统提供4种同步策略，决定文件冲突时怎么处理：

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| **最新优先** | 比较文件修改时间，新的覆盖旧的 | 日常使用，最常用 |
| **大小优先** | 比较文件大小，大小不同则同步 | 检测文件是否被修改 |
| **本地优先** | 强制以本地文件为准，上传到服务器 | 本地是主，服务器是备份 |
| **服务器优先** | 强制以服务器文件为准，下载到本地 | 服务器是主，本地是副本 |

**任务状态说明**

| 状态 | 含义 |
|------|------|
| 未同步 | 任务刚创建，还没开始同步 |
| 已同步 | 文件已经同步完成 |
| 同步中 | 正在同步文件 |
| 已停止 | 任务暂停，不自动同步 |

**操作说明**

- **启动**：开始监控本地文件夹，自动同步
- **暂停**：停止监控，不再自动同步
- **立即同步**：手动触发一次同步
- **删除**：删除任务（不会删除已同步的文件）

---

### 4. 忽略列表

**什么是忽略列表？**
- 有些文件你不想同步（如临时文件、缓存等）
- 通过设置忽略规则，这些文件会被跳过

**添加忽略规则**
1. 在同步任务列表中，点击"忽略列表"
2. 输入匹配规则
3. 点击"添加"

**规则语法（glob模式）**

| 规则 | 说明 | 示例 |
|------|------|------|
| `*.扩展名` | 匹配所有该扩展名的文件 | `*.svn`、`*.git`、`*.tmp` |
| `文件名` | 匹配特定文件名 | `Thumbs.db`、`.DS_Store` |
| `文件夹名` | 匹配特定文件夹 | `node_modules`、`__pycache__` |
| `前缀*` | 匹配以某字符串开头 | `temp*`、`backup*` |

**常用忽略规则推荐：**
- `*.svn` - SVN版本控制文件
- `*.git` - Git版本控制文件
- `node_modules` - Node.js依赖包
- `__pycache__` - Python缓存文件
- `*.tmp` - 临时文件
- `Thumbs.db` - Windows缩略图缓存
- `.DS_Store` - macOS系统文件

---

### 5. 文件浏览

**功能说明**
- 左侧：浏览本地同步文件夹
- 右侧：浏览FTP服务器文件夹
- 可以对比两边的文件是否一致

**使用方法**
1. 点击左侧菜单"文件浏览"
2. 左边下拉框选择同步任务（显示对应的本地文件夹）
3. 右边下拉框选择FTP服务器（显示服务器文件）
4. 点击文件夹可以进入子目录

---

### 6. 同步历史

**功能说明**
- 记录每次同步的详细信息
- 可以查看什么时候、同步了什么文件、怎么同步的

**历史记录字段**

| 字段 | 说明 |
|------|------|
| 文件名 | 被同步的文件名 |
| 同步方向 | 上传（本地→服务器）或下载（服务器→本地） |
| 操作类型 | 新增、更新、删除 |
| 同步时间 | 什么时候同步的 |
| 状态 | 成功或失败 |

**统计信息**
- 总同步次数
- 成功次数
- 失败次数
- 按方向统计（上传/下载）
- 按操作统计（新增/更新/删除）

---

### 7. 控制面板

**功能说明**
- 系统总览页面
- 显示关键统计信息

**显示内容**
1. **统计卡片**
   - FTP服务器数量
   - 同步任务数量
   - 成功同步次数
   - 失败同步次数

2. **FTP服务器状态**
   - 运行状态（运行中/已停止）
   - FTP根目录
   - 服务器地址
   - 默认账号信息

3. **最近同步历史**
   - 最近5条同步记录

4. **监控状态**
   - 哪些任务正在监控
   - 监控的文件夹路径

---

## 四、使用流程

### 第一次使用

```
1. 注册账号
   ↓
2. 登录系统
   ↓
3. 检查内置FTP服务器状态（控制面板）
   ↓
4. 添加FTP服务器（或使用内置的）
   ↓
5. 测试连接是否成功
   ↓
6. 创建同步任务
   ↓
7. 启动任务
   ↓
8. 在本地文件夹中放入文件，观察是否自动同步
```

### 日常使用

```
1. 确保后端服务在运行
   ↓
2. 打开浏览器访问系统
   ↓
3. 查看控制面板，确认FTP服务器运行正常
   ↓
4. 查看同步任务状态
   ↓
5. 如需手动同步，点击"立即同步"
   ↓
6. 查看同步历史，确认文件同步成功
```

---

## 五、常见问题

### Q1: 为什么文件没有自动同步？

**检查清单：**
1. 同步任务是否"已启动"？
2. FTP服务器是否"运行中"？
3. 本地路径是否正确？
4. 文件是否在忽略列表中？
5. 网络连接是否正常？

### Q2: 同步失败怎么办？

**可能原因：**
- FTP服务器连接失败 → 检查服务器配置
- 文件被占用 → 关闭正在使用的程序
- 权限不足 → 以管理员身份运行
- 磁盘空间不足 → 清理空间

### Q3: 如何修改同步策略？

1. 暂停当前任务
2. 删除任务
3. 重新创建任务，选择新的策略

### Q4: 如何同步到多台电脑？

1. 每台电脑安装本系统
2. 配置同一个FTP服务器
3. 每台电脑创建同步任务，指向同一个FTP服务器
4. 文件会自动在多台电脑间同步

### Q5: 内置FTP服务器是什么？

- 系统自带一个FTP服务器（端口2121）
- 默认账号：admin / admin123
- 用于测试和本地使用
- 生产环境建议使用专业的FTP服务器

---

## 六、技术参数

| 参数 | 值 |
|------|-----|
| 后端框架 | Flask |
| FTP服务器 | pyftpdlib |
| 文件监控 | watchdog |
| 定时任务 | APScheduler |
| 数据库 | SQLite |
| 前端框架 | Vue3 + Element Plus |
| 默认端口 | 后端5000，前端3000，FTP 2121 |

---

## 七、快捷操作

| 操作 | 方法 |
|------|------|
| 刷新页面 | F5 |
| 手动同步 | 点击"立即同步"按钮 |
| 暂停任务 | 点击"暂停"按钮 |
| 删除任务 | 点击"删除"按钮 |
| 查看历史 | 点击"同步历史"菜单 |
| 测试连接 | 点击"测试连接"按钮 |

---

## 八、注意事项

1. **文件锁定**：正在被其他程序打开的文件可能无法同步
2. **大文件**：大文件同步需要时间，请耐心等待
3. **网络**：FTP服务器需要网络可达
4. **路径**：本地路径使用Windows格式（如 `D:\folder`）
5. **重启**：重启电脑后需要重新启动后端服务

---

如有问题，请查看控制面板的状态信息，或检查同步历史中的错误详情。
